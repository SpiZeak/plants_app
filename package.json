{
  "name": "plants",
  "version": "0.0.0",
  "engines": {
    "node": "15.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "yarn build && serve -s dist"
  },
  "dependencies": {
    "serve": "^11.3.2",
    "vue": "^3.0.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.0.4",
    "@vue/compiler-sfc": "^3.0.5",
    "vite": "^2.0.0-beta.12"
  }
}
